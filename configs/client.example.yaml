id: dev-box
token: "dev-box-shared-secret-change-me"

# 服务端隧道接入地址。P1 用 ws://；P2 启用 TLS 后改为 wss://。
server_url: "ws://127.0.0.1:7443/api/tunnel"

# 项目默认根目录（相对路径基于客户端工作目录）。
workspace_root: "."

# SQLite 等本地状态存放目录。
data_dir: "./data"
